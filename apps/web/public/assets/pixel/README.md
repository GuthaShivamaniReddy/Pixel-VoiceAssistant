# Pixel mascot assets

| Path         | Role                                                         |
| ------------ | ------------------------------------------------------------ |
| `reference/` | Notes only. Do not place the supplied photo here for UI use. |
| `rive/`      | Reserved for a future Rive file. None shipped.               |
| `sprites/`   | Reserved for future original raster frames. None shipped.    |
| `icons/`     | Reserved.                                                    |

Production Pixel is an original shaded SVG illustration in `apps/web/src/mascot/PixelCharacter.tsx`.

The supplied photo was a look-and-feel guide (monitor head, green visor, blocky robot). It is not loaded by the application.
